# Server Configuration
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# Debug Configuration (optional - development için)
DEBUG_MODE=true
ENABLE_SWAGGER=true

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ecommerce_db
DB_USER=your_username
DB_PASSWORD=your_password

# JWT Configuration (⚠️ Production'da mutlaka değiştirin!)
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_REFRESH_SECRET=your-refresh-secret-key-change-in-production
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d

# Rate Limiting Configuration
RATE_LIMIT_WINDOW_SIZE=900000
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_LOGIN_MAX=5

# Logging Configuration
LOG_LEVEL=info
LOG_TO_FILE=true
LOG_DIR=./logs

# CSP Reporting (optional - domain aldığınızda)
# CSP_REPORT_URI=https://yourdomain.com/api/security/csp-report

# Security Configuration (optional)
BCRYPT_ROUNDS=10
SESSION_TIMEOUT=24
ENABLE_RATE_LIMIT_DEBUG=true

# Redis Configuration (optional - for scaling)
REDIS_URL=redis://localhost:6379
REDIS_PASSWORD=

# Email Configuration (📧 Yeni özellik!)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
# Gmail için App Password kullanın: https://support.google.com/accounts/answer/185833

# File Upload Configuration (optional)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
MAX_FILE_SIZE=10485760
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/webp

# Database Performance (optional)
DB_POOL_MIN=2
DB_POOL_MAX=10
DB_CONNECTION_TIMEOUT=5000

# Development Tools (optional)
ENABLE_CORS_ALL=false
ENABLE_REQUEST_LOGGING=true
API_DOCS_ENABLED=true

# Monitoring ve Analytics
ENABLE_METRICS=true
METRICS_CACHE_TTL=300000

# Security Headers
CSP_NONCE_ENABLED=true

# Development Helpers
ENABLE_DEBUG_ROUTES=false 